import { Button } from "@mui/material";
import React from "react";

const IndividualExperimentBarSummaryRightSection = () => {
  return (
    <Button
      variant="contained"
      color="primary"
      size="small"
      // onClick={() => setChooseWinnerOpen(true)}
    >
      Choose winner
    </Button>
  );
};

export default IndividualExperimentBarSummaryRightSection;
