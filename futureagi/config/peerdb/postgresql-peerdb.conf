# PostgreSQL config overrides for PeerDB CDC replication
# Appended to default postgresql.conf via command args
#
# NOTE: docker-compose.yml command args override these values.
# Keep both in sync. Currently 19 mirrors + 1 spare = 20.

wal_level = logical
max_wal_senders = 25
max_replication_slots = 25
