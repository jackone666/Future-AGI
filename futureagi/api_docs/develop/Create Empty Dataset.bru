meta {
  name: Create Empty Dataset
  type: http
  seq: 4
}

post {
  url: {{host}}/model-hub/develops/create-empty-dataset/
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  model_type: GenerativeLLM
  new_dataset_name: ds_0115
}
