meta {
  name: Add property to insight
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/model-hub/insight/add-property/
  body: json
  auth: bearer
}

params:query {
  :
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "newProperty" : ""
  }
}
