meta {
  name: Get Optimization Detail
  type: http
  seq: 6
}

get {
  url: http://localhost:8000/model-hub/optimize-dataset/{{model_id}}/{{optimization_id}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  model_id:
  optimization_id:
}
