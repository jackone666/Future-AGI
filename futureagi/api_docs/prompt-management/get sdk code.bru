meta {
  name: get sdk code
  type: http
  seq: 12
}

get {
  url: {{backend_url}}/model-hub/prompt-executions/:id/get-sdk-code/python
  body: none
  auth: bearer
}

params:path {
  id:
}

auth:bearer {
  token: {{access_token}}
}
