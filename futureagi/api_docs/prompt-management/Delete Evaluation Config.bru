meta {
  name: Delete Evaluation Config
  type: http
  seq: 22
}

delete {
  url: {{host}}/model-hub/prompt-templates/:id/delete_evaluation_configs/?id
  body: json
  auth: bearer
}

params:query {
  id:
}

params:path {
  id:
}

auth:bearer {
  token: {{access_token}}
}

tests {
  /*
  Test cases;

  1. Verified deleting selected eval template
  */
}
