meta {
  name: Get unique properties
  type: http
  seq: 2
}

get {
  url: {{host}}:{{port}}/model-hub/unique-properties/
  body: none
  auth: bearer
}

headers {
  ~Authorization:
}

auth:bearer {
  token: {{access_token}}
}
