eval_id: 28
name: contains_none
description: Verifies text contains none of specified terms
eval_tags:
- Output Validation
- Text
visible_ui: false
permissions:
  allow_edit: false
  allow_copy: true
output_type_normalized: pass_fail
pass_threshold: 0.5
config:
  required_keys:
  - text
  output: Pass/Fail
  eval_type_id: CustomCodeEval
  config:
    case_sensitive:
      type: boolean
      default: true
    keywords:
      type: list
      default: []
  config_params_desc:
    text: The input text to be evaluated
    case_sensitive: Whether the keyword search should be case-sensitive
    keywords: List of keywords that should not be present in the text
  code: |
    def evaluate(input, output, expected, context, **kwargs):
        text = kwargs.get("text", output or "")
        keywords = kwargs.get("keywords", [])
        case_sensitive = kwargs.get("case_sensitive", True)
        text = str(text)
        if isinstance(keywords, str):
            keywords = [k.strip() for k in keywords.split(",")]
        else:
            keywords = [str(k).strip() for k in keywords]
        if not keywords:
            return {"score": 1.0, "reason": "No keywords provided, vacuously true"}
        if not case_sensitive:
            text = text.lower()
            keywords = [k.lower() for k in keywords]
        found = [k for k in keywords if k in text]
        if found:
            return {"score": 0.0, "reason": "Keywords found: " + ", ".join(found)}
        else:
            return {"score": 1.0, "reason": "No keywords found in output"}
